postgres (postgreql / pgroll / pgloader)
    pgroll - zero-downtime schema changes (keeps 2 version of the schema accessible at the same time), reversible, schema migrations (using JSON format) for Postgres (CLI)
    pgloader - a data loading / database migration tool for PostgreSQL, using the COPY command.

keycloak 